version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - npm install -g pnpm typescript husky
      - pnpm install --frozen-lockfile

  pre_build:
    commands:
      - pnpm i --save-dev @types/node
      - npx prisma generate

  build:
    commands:
      - pnpm run build

artifacts:
  files:
    - 'package.json'
    - 'pnpm-lock.yaml'
    - 'build/**/*'
discard-paths: yes
